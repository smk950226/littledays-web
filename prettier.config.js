//  @ts-check

/** @type {import('prettier').Config} */
const config = {
    singleQuote: true,
    trailingComma: 'all',
    bracketSpacing: true,
    arrowParens: 'avoid',
    tabWidth: 4,
};

export default config;
